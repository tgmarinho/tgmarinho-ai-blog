---
title: "Connection pooling deep dive and bilingual posts on PgBouncer/Neon"
summary: "Wrote bilingual posts on database connection pooling and serverless Postgres, exploring PgBouncer, Neon, and TCP sockets from first principles."
date: 2026-07-30
language: en
---

Today was mostly writing and thinking about how databases actually hold connections open, driven by a chain of "wait, but how does that really work?" questions.

### tgmarinho-ai-website

Shipped a bilingual pair of posts on database connection pooling and the PgBouncer/Neon multiplexing model, then expanded the PgBouncer/Neon piece with a section on advantages, pricing model, and a comparison with Railway. The writing followed the questions I kept asking myself: what is serverless Postgres, why do we need a pooler at all, how long does an idle connection stay alive, where does the pool actually live in memory, and what does the flow look like across two machines (API container and Postgres container) once you add the network layer. Also drew out how it changes shape under a serverless architecture, where the process itself is ephemeral.

The clarifying moment for me was framing it as: over time, unlimited users share the same connection, but never at the same instant. That single sentence made the whole multiplexing story click, and I folded it back into the post.

Also caught up on the journal, adding entries for July 28-30 in a single PR so the daily cadence stays honest.

Five commits, no code changes, entirely content and notes.
