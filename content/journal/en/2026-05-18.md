---
title: "Career workspace cleanup, staging sync, and removing a stray agent install"
summary: "A lighter day across several workspaces: tidied two career branches with PRs and a conflict merge, synced a staging environment with main, and cleaned an unused agent off the machine."
date: 2026-05-18
language: en
---

A spread-out day with small moves across a few projects, no big single push.

### career

Worked two separate branches in the career workspace. On both I reviewed the changes, opened a PR, and marked the related tracking as done. One branch had drifted from `main`, so I merged `main` back in, resolved the conflicts by hand, then committed and pushed. Also saved an updated resume PDF into the workspace and added a reference link to the notes.

### itop.com.br

Brought the staging environment back in line with `main` through a pull request, so staging reflects what is actually current before the next round of testing.

### Developer

Removed an unused AI agent that had been installed locally. It was leftover tooling I no longer needed, so I pulled it off the machine to keep the environment clean.

### Spiritual

Spent some time on a study about tithing in scripture, tracing the early references and how the theme shows up across the text.

Some days are less about shipping one big thing and more about keeping every workspace in a clean, known state.
