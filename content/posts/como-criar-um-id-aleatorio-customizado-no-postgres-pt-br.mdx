---
title: "Como criar um ID aleatório customizado no Postgres"
description: "Função auxiliar para criar um ID aleatório customizado como a9Kl12"
date: "2023-02-15"
published: true
categories: ["database"]
image: "https://images.unsplash.com/photo-1603860785202-70ee34aa7b68?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=928&q=80"
language: "pt-BR"
slug: "como-criar-um-id-aleatorio-customizado-no-postgres"
translationKey: "how-to-create-a-custom-random-id-with-postgres"
---

Crie uma função:

```sql
CREATE OR REPLACE FUNCTION generate_random_id(length integer)
RETURNS text AS $$
DECLARE
  alphabet text;
  id text := '';
  i integer;
BEGIN
  alphabet := 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
  FOR i IN 1..length LOOP
    id := id || substr(alphabet, floor(random() * length(alphabet) + 1)::integer, 1);
  END LOOP;
  RETURN id;
END;
$$ LANGUAGE plpgsql;
```

Teste:

```sql
SELECT generate_random_id(10); -- retorna um ID aleatório com 10 caracteres
```


Agora você pode criar uma tabela desta forma:

```sql
CREATE TABLE "public"."product" ("id" text default generate_random_id(6), "name" text NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"));
```


Tente inserir e fazer um select na tabela product para ver o id gerado automaticamente.

```sql
INSERT INTO product (name) VALUES ('paper');
```


Pronto.
